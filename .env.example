# Local Gemini (AI Studio). Never commit the real key.
GEMINI_API_KEY=
# optional:
GEMINI_MODEL=gemini-2.5-flash

# Agent Engine deploy only. Local CLI ignores these.
GOOGLE_CLOUD_PROJECT=
GOOGLE_CLOUD_LOCATION=us-central1
